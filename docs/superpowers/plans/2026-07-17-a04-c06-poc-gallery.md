# A04 and C06 POC Gallery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add A04 and the latest C06 POCs to the main static `/poc` gallery and detail routes.

**Architecture:** Keep `pocGalleryData.ts` as the seven-card registry and `App.tsx` as route ownership. Port only the C06 component, scene model, original textures, provenance README, and normalized thumbnail; use Node's built-in test runner for C06 rather than importing the legacy Vitest configuration.

**Tech Stack:** React, React Router, React Three Fiber, Three.js, TypeScript, Node test runner, Vite.

---

## Task 1: Add C06 Runtime Assets and Model

**Files:**
- Create: `packages/sample/src/poc/C06DrilledHolePoC.tsx`
- Create: `packages/sample/src/poc/c06SceneModel.ts`
- Create: `packages/sample/src/poc/c06SceneModel.test.ts`
- Create: `packages/sample/public/textures/c06/README.md`
- Create: `packages/sample/public/textures/c06/aged-brick-albedo.png`
- Create: `packages/sample/public/textures/c06/broken-brick-core-albedo.png`

- [ ] Verify that commit `54228c2` contains the complete C06 implementation introduced by base commit `20120b6`, then port only the current runtime files, generated textures, and provenance README from that combined history.
- [ ] Rewrite the focused C06 assertions using `node:test` and `node:assert/strict`.
- [ ] Add a focused source assertion that the C06 texture root uses `import.meta.env.BASE_URL`, then run the C06 test to confirm Vite base-path-safe texture URLs, camera clamping, monotonic travel, fade behavior, and asymmetric brick layout.

### Task 2: Register Detail Routes and Gallery Cards

**Files:**
- Modify: `packages/sample/src/App.tsx`
- Modify: `packages/sample/src/poc/pocGalleryData.ts`
- Modify: `packages/sample/src/poc/pocGalleryData.test.ts`

- [ ] Write failing registry assertions for A04 and C06.
- [ ] Register `/poc/c06`, retain `/poc/a04`, and add both immutable card records.
- [ ] Run the registry test and confirm the seven-item order, unique routes, and local thumbnails.

### Task 3: Normalize and Pin Thumbnails

**Files:**
- Create: `packages/sample/public/poc-thumbnails/a04.png`
- Create: `packages/sample/public/poc-thumbnails/c06.png`
- Modify: `packages/sample/src/poc/pocGalleryData.test.ts`

- [ ] Convert the current generated POC previews to actual PNG files at 960x540.
- [ ] Write failing canonical-file assertions for both new thumbnails.
- [ ] Pin SHA-256 hashes only after the final normalized image files are in place.

### Task 4: Extend Provenance Policy

**Files:**
- Modify: `packages/sample/src/poc/pocProvenance.test.ts`
- Modify: `package.json`

- [ ] Add A04 and C06 dependency/asset policies, including C06's two exact generated texture files and the existing A04 CC0/generated asset directory.
- [ ] Add A04/C06 to the detail-module guard so the gallery remains a static index with no imported runtime POCs.
- [ ] Add `test:c06` to the root script and run the expanded provenance suite.

### Task 5: Verify and Commit

**Files:**
- Verify only; no new production files.

- [ ] Run `npm run test:c06`, `npm run test:poc-gallery`, `npm run lint`, `npm run build`, and `npm run gallery:check`.
- [ ] Verify `/poc`, `/poc/a04`, and `/poc/c06` in the local browser.
- [ ] Commit the integration as one focused conventional commit.
