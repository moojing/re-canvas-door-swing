// door_handles.glb is intentionally not exported — it is unused and exporting
// it would copy ~1 MB of dead weight into every consumer build.
export { default as doorHandleSingle } from "./door_handle_single.glb";
